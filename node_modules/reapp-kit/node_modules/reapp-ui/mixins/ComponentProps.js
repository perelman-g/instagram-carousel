// Uses a combination of all the internal mixins to provide
// a consistent set of props for each component

// ref, className, styles, and any animation styles

'use strict';

module.exports = {
  componentProps: function componentProps(ref) {
    ref = ref || 'self';

    var props = {
      ref: ref,
      className: this.getClassSet(ref),
      styles: this.getStyles(ref)
    };

    //this.isAnimating() &&
    if (this.hasAnimations(ref) && !this.animationsDisabled()) props.style = this.getAnimationStyle(ref);

    return props;
  }
};